@model IEnumerable<MVCPrueba.Models.Docente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nombre_Docente)
        </th>
  
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nombre_Docente)
        </td>
      
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Code_Docente }) |
            @Html.ActionLink("Details", "Details", new { id = item.Code_Docente }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Code_Docente })
        </td>
    </tr>
}

</table>
